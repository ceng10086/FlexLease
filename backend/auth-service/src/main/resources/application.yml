spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:h2:mem:flexlease-auth;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;INIT=CREATE SCHEMA IF NOT EXISTS AUTH
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        default_schema: auth
        format_sql: true
    open-in-view: false
  h2:
    console:
      enabled: true
      path: /h2-console
server:
  port: 9001
management:
  endpoints:
    web:
      exposure:
        include: health,info

security:
  jwt:
    secret: flexlease-default-secret-please-change
    access-token-ttl-seconds: 3600
    issuer: flexlease-auth-service

logging:
  level:
    org.springframework.security: INFO
