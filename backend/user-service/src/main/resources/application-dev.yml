spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:h2:mem:flexlease-user;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;INIT=CREATE SCHEMA IF NOT EXISTS USERS
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: users
        format_sql: true
    open-in-view: false
  flyway:
    enabled: true
    schemas: users
    locations: classpath:db/migration
  h2:
    console:
      enabled: true
      path: /h2-console
